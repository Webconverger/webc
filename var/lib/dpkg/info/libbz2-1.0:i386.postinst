#!/bin/sh
set -eu

# Ldconfig
if [ "$1" = "configure" ]; then
 ldconfig
fi

# FHS
#pkg=libbz2-1.0
#if [ "$1" = "configure" ]; then
# if [ -d /usr/doc ] && [ ! -e /usr/doc/$pkg ] && [ -d /usr/share/doc/$pkg ]; then
#  ln -sf ../share/doc/$pkg /usr/doc/$pkg
# fi
#fi
