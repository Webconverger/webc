#!/bin/sh
set -e
# Automatically added by dh_systemd_start
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop alsa-restore.service alsa-state.service >/dev/null
fi
# End automatically added section
